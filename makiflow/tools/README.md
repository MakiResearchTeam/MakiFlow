```python
parser = JsonParser()
parser.parse_coco_json('sample/instances_val2014.json')
```


# Пример использования tableParsera
```python
from makiflow.tools import TableParser
import os
import cv2

if __name__ == "__main__":
    table_one_labels = ['нахождение корня', 'прибавление', 'дифференциал', 'преобразование',
                        'множитель', 'дифференцируемый', 'ориентированный', 'сх-ть посл-ти',
                        'понижения', 'Последовательность', 'всех столбцов', 'Базис системы',
                        'множество', 'Противоречие.', 'Построение', 'объединение',
                        'аргумент числа', 'мощность мн-ва', 'Элемент этого', 'Функциональной',
                        'одну и ту же', 'бесконечная', 'тогда сумма', 'говорят, что',
                        'определенным', 'сходимость', 'равномерная', 'посл-ть',
                        'монотонна', 'фиксированных', 'угол между', 'Критерий сжатых',
                        'Тогда говорим', 'Степенной ряд', 'Радиус сх-ти', 'Теорема об']
    table_two_labels = ['из первого', 'имеет вид', 'Таким образом,', 'В соответствии',
                        'на примере', 'от функции', 'в детали', 'В итоге имеем',
                        'Складывая эти', 'Если ф-ия', 'Бета-функция', 'Пусть функция',
                        'и её частная', 'будем наз.', ', что поле', 'Коши задача',
                        'умножим и', 'Применим ф-лы', '2го рода', 'из выражения',
                        'на основе', 'если при обходе', 'Иными словами,', 'в свою очередь',
                        'Например,', 'т.е. для того', 'единичный вектор', 'В качестве',
                        'со стороны', 'можно считать', 'зависит от', 'число назад',
                        '- это биекция', 'знак меняется', 'число смен', 'Общий случай']
    table_three_labels = ['Множество А', 'Св-ва функций', 'группа по', 'не зависит',
                          'Две таблицы,', '- это сумма', 'Пусть матрица', 'эти строки',
                          'Док-во теоремы', 'аналогичное для', 'Пусть к-ая', 'вместо к-ой',
                          'Доп. минор', 'Все равно', 'Формула Лапласа', 'система из к',
                          'поле чисел', 'второй степени', 'выше первой', 'Дополнительный',
                          ', который', 'вещественных', 'существования', 'разделяющимися',
                          'Общие понятия', 'действующих', ', зависящие от', 'При помощи',
                          'Общий интеграл', 'интегрирующий', 'с помощью', ', разрешенных',
                          'систем линейных', 'всех корней', 'Нахождение всех', 'коэффициента']

    tables = [table_one_labels, table_two_labels, table_three_labels]

    parser = TableParser(tables, retr_feat=cv2.RETR_EXTERNAL, morph_feat=cv2.MORPH_CROSS)
    parser.crop_tables(os.path.join('before', 'dataset'), 'tables')
    parser.crop_cells('tables', 'cells')
```

_Will be soon..._